"""

Q8. Consider the following Python script written by a student to process a single image:

import cv2

# Operation 1: Loading the image
frame = cv2.imread("robot_vision.jpg")

# Operation 2: Converting color space
gray_frame = cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY)

# Operation 3: Drawing a bounding box
cv2.rectangle(gray_frame, (10, 10), (100, 100), (0, 255, 0), 3)

# Operation 4: Displaying the result
cv2.imshow("Robot Vision", gray_frame)
cv2.waitKey(1)

Which of the following statements are correct regarding how this script will execute?

A) By default, the cv2.imread() function in Operation 1 loads the image into a standard RGB
   (Red, Green, Blue) format, just like most other image libraries.

B) The rectangle drawn in Operation 3 will not appear green on the screen.
   Because gray_frame is a single-channel image, OpenCV cannot display color on it.

C) Operation 4 contains a logic error for viewing static images:
   cv2.waitKey(1) will only pause the window for 1 millisecond before the script
   finishes and the window vanishes.

D) The coordinates (10, 10) and (100, 100) in Operation 3 specify the center point
   of the rectangle and its total width/height, respectively.

Answer: B, C

"""